<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Disabled: reduced opacity so text looks disabled but stays visible -->
    <item android:color="@color/button_text_on_primary_disabled" android:state_enabled="false" />
    <!-- Enabled: full black text on primary (teal) button -->
    <item android:color="@color/black" />
</selector>
