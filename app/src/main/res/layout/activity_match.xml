<?xml version="1.0" encoding="utf-8"?>
<!--
  Match Found screen
  ROOT: LinearLayout (vertical — celebration card sits ABOVE the movie image)
    ├── MaterialCardView — "It's a Match!" celebration card (above image)
    └── FrameLayout (flex weight=1, takes remaining screen height)
          ├── ImageView      full-screen backdrop / poster (centerCrop, 16dp rounded)
          ├── View           darker gradient scrim (gradient_match_overlay)
          ├── LottieAnimationView  confetti overlay (pointer-transparent)
          └── ScrollView → LinearLayout (anchored bottom)
                ├── LinearLayout: title / date / rating / overview / genres
                ├── btn_watch_now  (filled teal pill)
                └── btn_find_another (outlined teal pill)
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container_match"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/color_background"
    tools:context=".ui.match.MatchActivity">

    <!-- Celebration card above the movie image -->
    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="10dp"
        app:cardBackgroundColor="@color/color_surface"
        app:cardCornerRadius="16dp"
        app:cardElevation="0dp">

        <!-- Outer: title row (left) + member count (right) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Top row: title | spacer | member count -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <TextView
                    android:id="@+id/text_celebration"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="@string/msg_match_found"
                    android:textColor="@color/color_primary"
                    android:textSize="22sp"
                    android:textStyle="bold"
                    tools:text="It's a Match!" />

                <!-- Member count cluster: icon + "2" -->
                <ImageView
                    android:layout_width="18dp"
                    android:layout_height="18dp"
                    android:src="@drawable/users_icon"
                    android:contentDescription="@null"
                    android:layout_marginStart="12dp"
                    app:tint="@color/color_text_secondary" />

                <TextView
                    android:id="@+id/text_member_count"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="4dp"
                    android:textColor="@color/color_text_secondary"
                    android:textSize="14sp"
                    android:textStyle="bold"
                    tools:text="2/2" />

            </LinearLayout>

            <!-- Subtitle — start-aligned below the row -->
            <TextView
                android:id="@+id/text_match_subtitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="Everyone agreed on this one!"
                android:textColor="@color/color_text_secondary"
                android:textSize="13sp" />

        </LinearLayout>

    </com.google.android.material.card.MaterialCardView>

    <!-- ── Movie image area (takes all remaining height) ─────────────────── -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <!-- Full-screen backdrop / poster -->
        <ImageView
            android:id="@+id/image_movie_poster"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:contentDescription="@string/label_movie_poster"
            tools:src="@color/color_surface" />

        <!-- Darker gradient scrim — ensures text is always readable -->
        <View
            android:id="@+id/view_card_scrim"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/gradient_match_overlay" />

        <!-- Confetti overlay — touch-transparent, plays once -->
        <com.airbnb.lottie.LottieAnimationView
            android:id="@+id/lottie_confetti"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clickable="false"
            android:focusable="false"
            app:lottie_fileName="confetti.json"
            app:lottie_loop="false"
            app:lottie_autoPlay="false" />

        <!-- Bottom info panel anchored to bottom of movie frame -->
        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:fillViewport="false"
            android:overScrollMode="never"
            android:scrollbars="none">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingStart="20dp"
                android:paddingEnd="20dp"
                android:paddingTop="24dp"
                android:paddingBottom="20dp">

                <!-- Movie info block (overlays the gradient scrim) -->
                <LinearLayout
                    android:id="@+id/layout_movie_info"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:layout_marginBottom="20dp">

                    <!-- Title -->
                    <TextView
                        android:id="@+id/text_movie_title"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:textColor="@color/white"
                        android:textSize="24sp"
                        android:textStyle="bold"
                        android:maxLines="3"
                        android:ellipsize="end"
                        tools:text="Dune: Part Two" />

                    <!-- Release date + rating row -->
                    <LinearLayout
                        android:id="@+id/row_release_date"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="8dp"
                        android:orientation="horizontal"
                        android:gravity="center_vertical">

                        <!-- Calendar icon -->
                        <ImageView
                            android:layout_width="14dp"
                            android:layout_height="14dp"
                            android:src="@drawable/calendar_icon"
                            android:contentDescription="@null"
                            app:tint="@color/color_text_secondary" />

                        <!-- Release date (formatted: "Feb 2025") -->
                        <TextView
                            android:id="@+id/text_movie_release_date"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_marginStart="4dp"
                            android:textColor="@color/color_text_secondary"
                            android:textSize="13sp"
                            tools:text="Feb 2025" />

                        <!-- Separator dot -->
                        <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_marginStart="10dp"
                            android:layout_marginEnd="10dp"
                            android:text="•"
                            android:textColor="@color/color_text_secondary"
                            android:textSize="13sp" />

                        <!-- Star icon -->
                        <ImageView
                            android:layout_width="14dp"
                            android:layout_height="14dp"
                            android:src="@drawable/star_icon"
                            android:contentDescription="@null"
                            app:tint="@color/color_secondary" />

                        <!-- Rating value -->
                        <TextView
                            android:id="@+id/text_movie_rating"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_marginStart="4dp"
                            android:textColor="@color/color_secondary"
                            android:textSize="13sp"
                            android:textStyle="bold"
                            tools:text="8.4" />

                    </LinearLayout>

                    <!-- Overview -->
                    <TextView
                        android:id="@+id/text_movie_overview"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="12dp"
                        android:textColor="#CCFFFFFF"
                        android:textSize="13sp"
                        android:lineSpacingMultiplier="1.4"
                        tools:text="Follow the mythic journey of Paul Atreides as he unites with Chani and the Fremen while on a warpath of revenge." />


                    <!-- Genre chips -->
                    <com.google.android.material.chip.ChipGroup
                        android:id="@+id/chip_group_genres"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="8dp"
                        app:singleLine="false"
                        app:chipSpacingVertical="-9dp"
                        app:chipSpacingHorizontal="5dp" />

                </LinearLayout>

                <!-- ── HOST actions: Watch Now (full-width) + Find Another Match ── -->
                <LinearLayout
                    android:id="@+id/layout_host_actions"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:visibility="visible">

                    <!-- Watch Now — full-width filled teal pill -->
                    <com.google.android.material.card.MaterialCardView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        app:cardBackgroundColor="@color/color_surface"
                        app:cardCornerRadius="50dp"
                        app:cardElevation="4dp">

                        <com.google.android.material.button.MaterialButton
                            android:id="@+id/btn_watch_now"
                            android:layout_width="match_parent"
                            android:layout_height="64dp"
                            android:text="Watch Now"
                            android:textColor="@color/black"
                            android:textSize="16sp"
                            android:textStyle="bold"
                            app:backgroundTint="@color/color_primary"
                            app:cornerRadius="50dp" />

                    </com.google.android.material.card.MaterialCardView>

                    <!-- Find Another Match — text link -->
                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/btn_find_another"
                        style="@style/Widget.Material3.Button.TextButton"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_gravity="center_horizontal"
                        android:layout_marginTop="4dp"
                        android:layout_marginBottom="4dp"
                        android:text="Find Another Match"
                        android:textColor="@color/color_text_secondary"
                        android:textSize="14sp" />

                </LinearLayout>

                <!-- ── MEMBER: wait info text only (no buttons) ── -->
                <LinearLayout
                    android:id="@+id/layout_member_actions"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:gravity="center_horizontal"
                    android:visibility="gone">

                    <TextView
                        android:id="@+id/text_member_wait_info"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="8dp"
                        android:layout_marginBottom="8dp"
                        android:text="Wait for the host to start watch the movie"
                        android:textColor="@color/color_text_secondary"
                        android:textSize="13sp"
                        android:gravity="center" />

                </LinearLayout>

            </LinearLayout>

        </ScrollView>

    </FrameLayout>

</LinearLayout>
