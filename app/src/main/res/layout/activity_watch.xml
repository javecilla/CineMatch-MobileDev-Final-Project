<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container_watch"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/color_background"
    tools:context=".ui.watch.WatchActivity">

    <!-- Movie Poster Background -->
    <ImageView
        android:id="@+id/image_movie_poster"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:contentDescription="@null" />

    <!-- Dark gradient overlay to make text readable -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/gradient_match_overlay" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Info message: "You, and 2 others are watching!" -->
        <TextView
            android:id="@+id/text_watch_info"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingStart="20dp"
            android:paddingEnd="20dp"
            android:paddingTop="32dp"
            android:paddingBottom="16dp"
            android:textColor="@color/white"
            android:textSize="16sp"
            android:textStyle="bold"
            android:gravity="center"
            tools:text="You, and 2 others are watching!" />

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:fillViewport="true"
            android:overScrollMode="never"
            android:scrollbars="none">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingStart="20dp"
                android:paddingEnd="20dp"
                android:gravity="bottom">

                <Space
                    android:layout_width="match_parent"
                    android:layout_height="32dp" />

                <!-- Video area -->
                <com.google.android.material.card.MaterialCardView
                    android:layout_width="match_parent"
                    android:layout_height="200dp"
                    android:layout_marginBottom="24dp"
                    app:cardBackgroundColor="@color/color_surface"
                    app:cardCornerRadius="12dp"
                    app:cardElevation="4dp">

                    <VideoView
                        android:id="@+id/video_intro"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:layout_gravity="center" />
                </com.google.android.material.card.MaterialCardView>

                <!-- Actions: Play (host only) + Done Watching -->
                <LinearLayout
                    android:id="@+id/layout_watch_actions"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:layout_marginBottom="16dp">

                    <LinearLayout
                        android:id="@+id/layout_host_actions"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="vertical">

                        <!-- Play button – same style as MatchActivity host Watch Now -->
                        <com.google.android.material.card.MaterialCardView
                            android:id="@+id/card_play"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            app:cardBackgroundColor="@color/color_surface"
                            app:cardCornerRadius="50dp"
                            app:cardElevation="4dp">

                            <com.google.android.material.button.MaterialButton
                                android:id="@+id/btn_toggle_playback"
                                android:layout_width="match_parent"
                                android:layout_height="64dp"
                                android:text="@string/btn_play"
                                android:textColor="@color/black"
                                android:textSize="16sp"
                                android:textStyle="bold"
                                app:icon="@drawable/play_icon"
                                app:iconTint="@color/black"
                                app:iconGravity="textStart"
                                app:backgroundTint="@color/color_primary"
                                app:cornerRadius="50dp" />

                        </com.google.android.material.card.MaterialCardView>

                        <!-- Done Watching – text button -->
                        <com.google.android.material.button.MaterialButton
                            android:id="@+id/btn_done_watching"
                            style="@style/Widget.Material3.Button.TextButton"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_gravity="center_horizontal"
                            android:layout_marginTop="4dp"
                            android:text="@string/btn_done_watching"
                            android:textColor="@color/color_text_secondary"
                            android:textSize="14sp" />

                    </LinearLayout>

                    <!-- Wait info — only visible for members -->
                    <TextView
                        android:id="@+id/text_member_status"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_gravity="center_horizontal"
                        android:layout_marginTop="12dp"
                        android:layout_marginBottom="4dp"
                        android:text="@string/msg_wait_host_play"
                        android:textColor="@color/color_text_secondary"
                        android:textSize="14sp"
                        android:visibility="gone"
                        android:gravity="center" />

                </LinearLayout>

                <LinearLayout
                    android:id="@+id/layout_movie_info"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:layout_marginBottom="32dp">


                <!-- Title -->
                <TextView
                    android:id="@+id/text_movie_title"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textColor="@color/white"
                    android:textSize="22sp"
                    android:textStyle="bold"
                    android:maxLines="3"
                    android:ellipsize="end"
                    tools:text="Inception" />

                <!-- Release date + rating row -->
                <LinearLayout
                    android:id="@+id/row_release_date"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:orientation="horizontal"
                    android:gravity="center_vertical">

                    <ImageView
                        android:layout_width="14dp"
                        android:layout_height="14dp"
                        android:src="@drawable/calendar_icon"
                        android:contentDescription="@null"
                        app:tint="@color/color_text_secondary" />

                    <TextView
                        android:id="@+id/text_movie_release_date"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="4dp"
                        android:textColor="@color/color_text_secondary"
                        android:textSize="13sp"
                        tools:text="Jul 2010" />

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="10dp"
                        android:layout_marginEnd="10dp"
                        android:text="•"
                        android:textColor="@color/color_text_secondary"
                        android:textSize="13sp" />

                    <ImageView
                        android:layout_width="14dp"
                        android:layout_height="14dp"
                        android:src="@drawable/star_icon"
                        android:contentDescription="@null"
                        app:tint="@color/color_secondary" />

                    <TextView
                        android:id="@+id/text_movie_rating"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="4dp"
                        android:textColor="@color/color_secondary"
                        android:textSize="13sp"
                        android:textStyle="bold"
                        tools:text="8.8" />

                </LinearLayout>

                <!-- Overview -->
                <TextView
                    android:id="@+id/text_movie_overview"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="12dp"
                    android:textColor="#CCFFFFFF"
                    android:textSize="13sp"
                    android:lineSpacingMultiplier="1.4"
                    tools:text="A thief who steals corporate secrets through the use of dream-sharing technology is given the inverse task of planting an idea into the mind of a CEO." />

                <!-- Genre chips -->
                <com.google.android.material.chip.ChipGroup
                    android:id="@+id/chip_group_genres"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    app:singleLine="false"
                    app:chipSpacingVertical="-9dp"
                    app:chipSpacingHorizontal="5dp" />

            </LinearLayout>
            </LinearLayout>
        </ScrollView>
    </LinearLayout>
</FrameLayout>
